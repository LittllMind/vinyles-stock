# T13.3 — Corrections Préparées (au cas où)

## 🔧 Fix IDOR UserController

### `app/Http/Controllers/UserController.php`

Ajouter la vérification propriétaire dans `edit()`, `update()`, `updatePassword()` :

```php
public function edit(User $user)
{
    if ($user->id !== auth()->id()) {
        abort(403, 'Vous ne pouvez pas modifier ce profil.');
    }
    return view('users.edit', compact('user'));
}

public function update(Request $request, User $user)
{
    if ($user->id !== auth()->id()) {
        abort(403, 'Vous ne pouvez pas modifier ce profil.');
    }
    // ... validation et update
}

public function updatePassword(Request $request, User $user)
{
    if ($user->id !== auth()->id()) {
        abort(403, 'Vous ne pouvez pas modifier ce profil.');
    }
    // ... validation et update
}
```

## 🔧 Fix IDOR Fonds (si nécessaire)

Si `test_idor_on_fonds_returns_403` échoue, ajouter dans `StockMovementController::updateStock()` :

```php
if (!auth()->user()->isAdmin()) {
    abort(403, 'Accès réservé aux administrateurs.');
}
```

---

*Corrections prêtes à appliquer selon résultats tests*
